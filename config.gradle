ext {
// app 相关版本控制
    versions = [
            minSdkVersion :22,
            targetSdkVersion :29,
            compileSdkVersion :29,
            buildToolsVersion :"29.0.3",
            appVersionCode :1,
            appVersionName : "1.0.1"
    ]
// 依赖相关版本控制
    depVersion = [
    ]
// 依赖
    deps = [
    ]
}